# 数据集可视化验证指南

> 完整的数据集验证流程，确保训练前数据集标注正确

---

## 📋 目录

- [为什么需要验证](#为什么需要验证)
- [快速开始](#快速开始)
- [验证工具说明](#验证工具说明)
- [验证结果解读](#验证结果解读)
- [常见问题处理](#常见问题处理)
- [最佳实践](#最佳实践)

---

## 为什么需要验证

在开始训练之前，验证数据集的正确性至关重要：

### ❌ 不验证的风险

- **标注错误**: 边界框位置不正确，导致模型学习错误特征
- **坐标超界**: 边界框超出图像边界，导致训练崩溃或性能下降
- **时间戳不匹配**: 图像和标注时间戳不对应，导致标注错位
- **数据缺失**: 部分图像没有标注，或标注文件损坏
- **格式错误**: COCO 格式不正确，导致数据加载失败

### ✅ 验证的好处

- **提前发现问题**: 在训练前发现并修复数据问题
- **节省时间**: 避免因数据问题导致的无效训练
- **提高性能**: 确保模型在高质量数据上训练
- **可视化确认**: 直观查看标注是否正确

---

## 快速开始

### 方法1: 交互式验证（推荐）

```bash
# 运行交互式验证脚本
bash validate_dataset.sh
```

然后根据提示选择验证模式：

```
请选择验证模式:
1) RGB训练集 (20个样本)
2) RGB验证集 (20个样本)
3) RGB测试集 (20个样本)
4) Event训练集 (20个样本)
5) Event验证集 (20个样本)
6) Event测试集 (20个样本)
7) RGB所有划分 (train/val/test)
8) Event所有划分 (train/val/test)
9) 全部验证 (RGB+Event, train/val/test)
0) 自定义参数

请输入选项 [1-9, 0]: 
```

### 方法2: 命令行验证

```bash
# 验证 RGB 训练集（20个样本）
python visualize_dataset_validation.py --modality rgb --split train --num_samples 20

# 验证 Event 验证集（10个样本）
python visualize_dataset_validation.py --modality event --split val --num_samples 10

# 验证所有数据集（RGB+Event, train/val/test）
python visualize_dataset_validation.py --modality both --split all --num_samples 20
```

### 方法3: 使用其他验证脚本

```bash
# 检查边界框有效性（不生成图片，速度快）
python scripts/visualize_coco_samples.py --modality rgb --split train --check_only

# 可视化样本（生成图片）
python scripts/visualize_coco_samples.py --modality rgb --split train --num_samples 10

# 验证时间戳匹配
python scripts/visualize_dataset.py --compare_original --video_id 3
```

---

## 验证工具说明

### 主验证工具: `visualize_dataset_validation.py`

**功能**:
- ✅ 可视化数据集样本（绘制边界框）
- ✅ 检查边界框有效性（超界、过小、过大）
- ✅ 统计边界框尺寸分布
- ✅ 生成 HTML 可视化报告
- ✅ 生成 JSON 详细报告

**参数说明**:

```bash
python visualize_dataset_validation.py \
    --coco_root /mnt/data/datasets/fred \      # FRED原始数据集路径
    --output_root datasets/fred_coco \         # COCO数据集路径
    --modality rgb \                           # 模态: rgb/event/both
    --split train \                            # 划分: train/val/test/all
    --num_samples 20 \                         # 每个划分的样本数
    --output_dir dataset_validation \          # 输出目录
    --seed 42                                  # 随机种子
```

### 辅助验证工具

#### 1. `scripts/visualize_coco_samples.py`

**特点**: 快速检查边界框质量

```bash
# 仅检查（不生成图片，速度快）
python scripts/visualize_coco_samples.py --modality rgb --split train --check_only

# 可视化样本
python scripts/visualize_coco_samples.py --modality rgb --split train --num_samples 10
```

**输出示例**:
```
======================================================================
检查边界框有效性 - RGB TRAIN
======================================================================
总标注数: 13629
有效标注: 13200 (96.85%)

问题统计:
  负坐标: 0
  零面积: 0
  超出边界: 429
  太小(<5px): 0
  太大(>90%): 0
======================================================================
```

#### 2. `scripts/visualize_dataset.py`

**特点**: 验证时间戳匹配

```bash
# 可视化样本
python scripts/visualize_dataset.py --modality rgb --split train --num_samples 5

# 对比原始数据（验证时间戳）
python scripts/visualize_dataset.py --compare_original --video_id 3
```

---

## 验证结果解读

### 输出目录结构

```
dataset_validation/
├── rgb_train/                      # RGB 训练集验证结果
│   ├── 0001_Video_0_*.jpg         # 可视化图片（带边界框）
│   ├── 0002_Video_0_*.jpg
│   ├── ...
│   ├── validation_report.json     # JSON 格式验证报告
│   └── validation_report.html     # HTML 格式验证报告 ⭐
│
├── rgb_val/                        # RGB 验证集
├── rgb_test/                       # RGB 测试集
├── event_train/                    # Event 训练集
├── event_val/                      # Event 验证集
└── event_test/                     # Event 测试集
```

### HTML 报告

**打开方式**:
```bash
# Firefox
firefox dataset_validation/rgb_train/validation_report.html

# Chrome
google-chrome dataset_validation/rgb_train/validation_report.html

# 或直接双击文件
```

**报告内容**:

1. **统计信息**
   - 总图像数
   - 总标注数
   - 有效边界框数
   - 问题边界框数（超界、过小、过大）

2. **可视化图片网格**
   - 每张图片显示边界框
   - 绿色框 = 正常
   - 红色框 = 有问题
   - 显示边界框尺寸（宽x高）

3. **问题标注高亮**
   - 黄色背景标记有问题的图片
   - 显示具体问题类型

### JSON 报告

**查看方式**:
```bash
cat dataset_validation/rgb_train/validation_report.json
```

**报告结构**:
```json
{
  "timestamp": "2025-10-31T12:34:56",
  "modality": "rgb",
  "split": "train",
  "num_samples": 20,
  "statistics": {
    "total_images": 13629,
    "total_annotations": 13629,
    "valid_bboxes": 13200,
    "out_of_bounds": 429,
    "too_small": 0,
    "too_large": 0,
    "bbox_sizes": [[50.2, 34.1], ...],
    "bbox_areas": [1712.3, ...]
  },
  "validation_results": [
    {
      "image_id": 1,
      "filename": "1/PADDED_RGB/Video_0_16_03_03.363444.jpg",
      "num_annotations": 1,
      "status": "OK",
      "bbox_issues": null
    },
    {
      "image_id": 2,
      "filename": "1/PADDED_RGB/Video_0_16_03_05.363444.jpg",
      "num_annotations": 1,
      "status": "WARNING",
      "bbox_issues": [
        {
          "bbox": [640.5, 360.2, 50.3, 34.1],
          "issues": ["out_of_bounds"]
        }
      ]
    }
  ]
}
```

### 可视化图片

**图片命名**: `0001_<原始文件名>.jpg`

**图片内容**:
- 原始图像
- 边界框（绿色=正常，红色=有问题）
- 边界框尺寸标签（W:宽 H:高）
- 中心点标记（蓝色圆点）
- 图像信息（ID、尺寸、标注数）
- 时间戳信息（如果有）
- 警告信息（如果有问题）

---

## 验证指标说明

### 边界框质量检查

| 检查项 | 说明 | 判定标准 | 标记颜色 |
|-------|------|---------|---------|
| **有效边界框** | 坐标正确，尺寸合理 | 通过所有检查 | 🟢 绿色 |
| **超出边界** | 边界框超出图像边界 | x < 0 或 y < 0 或 x+w > width 或 y+h > height | 🔴 红色 |
| **过小** | 边界框太小 | width < 5px 或 height < 5px | 🔴 红色 |
| **过大** | 边界框太大 | width > 90% 或 height > 90% | 🔴 红色 |

### 统计指标

**边界框尺寸统计**:
- 平均宽度/高度
- 中位数宽度/高度
- 最小/最大宽度/高度
- 平均面积

**数据集统计**:
- 总图像数
- 总标注数
- 有标注的图像数
- 无标注的图像数

---

## 常见问题处理

### 问题1: 边界框超出边界

**现象**:
```
超出边界: 429
```

**原因**:
- 原始标注超出图像边界
- 坐标转换错误

**处理**:
- ✅ **轻微超界** (< 5%): 可以接受，训练时会自动裁剪
- ⚠️ **严重超界** (> 10%): 需要检查数据转换脚本

**验证**:
```bash
# 查看具体超界的图片
python scripts/visualize_coco_samples.py --modality rgb --split train --num_samples 50
```

### 问题2: 边界框过小

**现象**:
```
过小 (<5px): 15
```

**原因**:
- 目标确实很小
- 标注错误

**处理**:
1. 查看可视化图片，确认是否为真实小目标
2. 如果是标注错误，需要重新标注
3. 如果是真实小目标，考虑：
   - 增大输入尺寸（如 640 -> 1280）
   - 使用小目标检测策略

### 问题3: 图片不存在

**现象**:
```
⚠️  图片不存在: /mnt/data/datasets/fred/1/PADDED_RGB/Video_0_*.jpg
```

**原因**:
- COCO 标注中的 `file_name` 路径不正确
- 原始图片文件缺失

**处理**:
```bash
# 检查 COCO 标注文件
cat datasets/fred_coco/rgb/annotations/instances_train.json | grep "file_name" | head -5

# 检查原始图片是否存在
ls /mnt/data/datasets/fred/1/PADDED_RGB/ | head -5

# 如果路径不对，重新转换数据集
python convert_fred_to_coco.py --modality rgb
```

### 问题4: 时间戳不匹配

**现象**:
```
⚠️  时间戳匹配可能有问题，时间差 0.15s > 0.05s
```

**原因**:
- 图像帧率和标注帧率不一致
- 时间戳计算错误

**处理**:
```bash
# 验证时间戳匹配
python scripts/visualize_dataset.py --compare_original --video_id 3

# 如果时间差过大，调整容差
python convert_fred_to_coco.py --modality rgb --time_tolerance 0.1
```

### 问题5: 无标注图像

**现象**:
```
无标注图像数: 150
```

**原因**:
- 部分图像确实没有目标
- 时间戳匹配失败

**处理**:
1. 查看 `validation_report.json`，找到无标注的图像
2. 检查原始 `coordinates.txt` 是否有对应时间戳的标注
3. 如果是时间戳匹配失败，调整 `time_tolerance` 参数

---

## 最佳实践

### 验证流程建议

#### 第一步: 快速检查

```bash
# 仅检查边界框质量（不生成图片，速度快）
python scripts/visualize_coco_samples.py --modality rgb --split train --check_only
python scripts/visualize_coco_samples.py --modality event --split train --check_only
```

**目标**: 快速了解数据集整体质量

#### 第二步: 可视化验证

```bash
# 可视化少量样本（20个）
python visualize_dataset_validation.py --modality rgb --split train --num_samples 20
python visualize_dataset_validation.py --modality event --split train --num_samples 20
```

**目标**: 直观查看标注是否正确

#### 第三步: 查看 HTML 报告

```bash
# 在浏览器中打开
firefox dataset_validation/rgb_train/validation_report.html
firefox dataset_validation/event_train/validation_report.html
```

**目标**: 全面了解数据集质量和问题

#### 第四步: 处理问题

根据验证结果，处理发现的问题：
- 超界问题 → 检查是否可接受
- 时间戳问题 → 调整容差或重新转换
- 标注错误 → 修正原始标注

#### 第五步: 重新验证

```bash
# 修复问题后，重新验证
python visualize_dataset_validation.py --modality both --split all --num_samples 20
```

### 验证频率建议

| 场景 | 验证频率 | 样本数 |
|------|---------|-------|
| **首次使用数据集** | 必须验证 | 50+ 样本 |
| **数据集更新后** | 必须验证 | 20+ 样本 |
| **训练前** | 建议验证 | 10+ 样本 |
| **训练效果差** | 必须验证 | 50+ 样本 |
| **定期检查** | 可选 | 5+ 样本 |

### 验证标准

#### ✅ 数据集合格标准

- 有效边界框 > 95%
- 超出边界 < 5%
- 过小边界框 < 1%
- 无标注图像 < 5%
- 时间戳匹配率 > 95%

#### ⚠️ 需要注意

- 有效边界框 90-95%
- 超出边界 5-10%
- 过小边界框 1-3%

#### ❌ 需要修复

- 有效边界框 < 90%
- 超出边界 > 10%
- 过小边界框 > 3%
- 无标注图像 > 10%

---

## 快速命令参考

### 常用验证命令

```bash
# 1. 快速检查（推荐首次使用）
bash validate_dataset.sh

# 2. 检查边界框质量（快速）
python scripts/visualize_coco_samples.py --modality rgb --split train --check_only

# 3. 可视化验证（详细）
python visualize_dataset_validation.py --modality rgb --split train --num_samples 20

# 4. 验证所有数据集
python visualize_dataset_validation.py --modality both --split all --num_samples 20

# 5. 验证时间戳匹配
python scripts/visualize_dataset.py --compare_original --video_id 3
```

### 查看验证结果

```bash
# 1. 浏览器查看 HTML 报告（推荐）
firefox dataset_validation/rgb_train/validation_report.html

# 2. 查看 JSON 报告
cat dataset_validation/rgb_train/validation_report.json | jq

# 3. 查看可视化图片
ls dataset_validation/rgb_train/*.jpg
```

---

## 总结

### 验证的重要性

> **在训练前验证数据集，可以节省大量时间和计算资源！**

- ✅ 提前发现数据问题
- ✅ 确保标注正确
- ✅ 提高训练效果
- ✅ 避免无效训练

### 推荐验证流程

1. **快速检查** → `bash validate_dataset.sh`
2. **查看 HTML 报告** → 浏览器打开 `validation_report.html`
3. **处理问题** → 根据报告修复问题
4. **重新验证** → 确认问题已解决
5. **开始训练** → 使用验证通过的数据集

---

**最后更新**: 2025-10-31  
**相关文档**: `README.md`, `AGENTS.md`
